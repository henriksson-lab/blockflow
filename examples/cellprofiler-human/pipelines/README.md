# Pipelines

The benchmark uses CellProfiler's `ExampleHuman.cppipe` from the upstream
CellProfiler examples archive. It is downloaded by
`../scripts/fetch_cellprofiler_human.sh` and is not vendored here.
