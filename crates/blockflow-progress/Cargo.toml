[package]
name = "blockflow-progress"
version = "0.1.0"
edition = "2021"
rust-version = "1.85"
license = "MIT"
description = "Portable JSONL envelope for live Blockflow progress streams."

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
tempfile = "3"
